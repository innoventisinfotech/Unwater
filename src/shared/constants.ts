/** Shared constants used across the main/preload/renderer boundary. */

/** Response returned by the `app:ping` IPC handler — used to prove the preload bridge works. */
export const PING_RESPONSE = 'pong'
